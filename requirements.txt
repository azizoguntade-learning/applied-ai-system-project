# Testing
pytest>=7.0              # 7.0+ required for the `pythonpath` ini option

# No runtime dependencies. The recommender is fully deterministic and offline:
# it uses only the Python standard library (csv, logging, argparse, json).

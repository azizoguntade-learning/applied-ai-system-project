# Byte compiled
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
env/
venv/

# OS files
.DS_Store

# Pytest cache
.pytest_cache/

# Secrets — never commit a real API key. See .env.example for the template.
.env

# Live log output — rewritten on every run, so not worth tracking.
logs/run.log
logs/traces.jsonl

# NOTE: logs/evidence/ is deliberately NOT ignored. Those curated snapshots are
# the reproducible execution evidence referenced from README.md.
